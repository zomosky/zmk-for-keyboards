# This file generates the GitHub Actions matrix
# For simple board + shield combinations, add them
# to the top level board and shield arrays, for more
# control, add individual board + shield combinations to
# the `include` property, e.g:
#
# board: [ "nice_nano_v2" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano_v2
#     shield: reviung41
#
---
#使用nice view和oled的固件不同，请根据提示修改该配置页
#使用nice view则将*shield：lily58_left*  *shield：lily58_right*进行注释
#使用oled则将*shield：lily58_left nice_view_adapter nice_view*  *shield: lily58_right nice_view_adapter nice_view*进行注释

include:
  - board: nice_nano_v2
    shield: lily58_left nice_view_adapter nice_view  
#   shield: lily58_left  
    snippet: studio-rpc-usb-uart
  - board: nice_nano_v2
    shield: lily58_right nice_view_adapter nice_view
#   shield: lily58_right
  - board: nice_nano_v2
    shield: settings_reset
